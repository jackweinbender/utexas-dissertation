\ProvidesPackage{pandoc-polyfill}[2016/08/08 Pandoc polyfill]

% Pandoc compatability for tightlist command
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Allows or links in document, but doesn't show ugly borders, etc.
  \usepackage[hidelinks]{hyperref}